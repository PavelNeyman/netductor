# Client profiles

## SR Config (Mac + OpenConnect)

File on primary: `/opt/netductor/profiles/nd-oc.conf`

**Delivery:** Telegram bot → Users → **Pavel** → Access → **SR Config** (same row as VLESS/Core/HY2).
Sends the file as a **Telegram document** (no public HTTP download required).

Usage: Shadowrocket → Config → import file. OpenConnect first, Global Routing = Config.
