# Bootstrap

```bash
export NETDUCTOR_VERSION=0.8.1
curl -fsSL https://raw.githubusercontent.com/PavelNeyman/netductor/main/bootstrap.sh | bash
```

Ставит `/usr/local/bin/netductor` из релиза **`v0.8.1`** (переменная `NETDUCTOR_VERSION`).

EN: [BOOTSTRAP.md](../BOOTSTRAP.md).
