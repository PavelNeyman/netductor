# Ревью 2026-09-14

## Обновлено
- Relay agent binary на RU VPS
- Core + TG bot

## Mismatch
- Core journal: счётчик работает (`netductor vpn mismatch`)
- Relay сейчас 0 (нет mismatch в journal за 30м) — agent шлёт total при heartbeat
- Типичный IP 46.147.x — домашний NAT / клиенты

## Security fixes (этот проход)
- `/api/edge/cmd` — только session (не device token)
- `/api/edge/devices` — только session
- `/api/edge/rsc` — device token или session; path traversal guard
- Relay provision SSH — TOFU known_hosts

## Остаточный риск (приемлемо)
- TG admin = numeric id (private chat)
- :8788 public for relay agents (token auth)
- Lampac/bind explicit
