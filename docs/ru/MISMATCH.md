# Flow mismatch

```bash
netductor vpn mismatch
```

Статус TG / API `/api/status` → поле `mismatch` (core journal) и `relay_mismatch` (с heartbeat агента).

Ненулевой total с вашего IP → клиент без Vision или битый Shadowrocket-конфиг.
