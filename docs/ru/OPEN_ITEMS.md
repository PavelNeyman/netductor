# Открытые темы

## Закрыто / в работе
- [x] Unified nodeCard + native TG tables (Bot API 10.1 sendRichMessage)
- [x] Core/relay upgrade + TG notify
- [x] Dual VPN link (relay primary + core home)
- [x] Status screen as tables
- [ ] Relay notify надёжность при долгом apt (частично: immediate cmd_done)
- [ ] OpenWrt agent: healthcheck relay → WAN fallback
- [ ] Full Admin SPA parity с TG (provision, cmd, alerts badge)
- [ ] SNI presets под Yota/МегаФон при включении БС
- [ ] MikroTik agent (npk) — отложено
- [ ] HTTPS/домен для API — отложено
- [ ] Mini App — отложено

## Управление нодами — backlog
1. Per-node actions: restart service, view journal tail, disk/net IO
2. Scheduled upgrade window + dry-run
3. Node labels/tags (city, role secondary)
4. Config drift check (desired vs applied version)
5. One-click snapshot / backup pull to core
6. Bandwidth / concurrent sessions graph in Admin
7. Alert rules UI (thresholds, mute)
8. Multi-core / second exit node
