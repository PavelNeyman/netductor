# Edge-агент (OpenWrt)

См. [EDGE-AGENT.md](../EDGE-AGENT.md).

- Конфиг: `/etc/netductor-agent/config` (`SERVER`, `TOKEN`, `DEVICE_ID`, `CONTROL_ONLY`).
- Recovery UI: `:7879/netductor-recovery`.
- Обновление агента: команда `agent_update` с URL|sha256 с primary (точечно, не mesh-auto).
