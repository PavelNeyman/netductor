# Admin UI

`http://127.0.0.1:8787/admin/` (SSH tunnel).

Session: `netductor vpn session 72` or Telegram.

Tabs: Overview, VPN, Metrics, Routers (edge), Probes, Settings.

## Edge recovery

Routers section: **Issue recovery code**, **Register pending**, **Set site**, **Export devices**.
LAN page on agent `:7879/netductor-recovery` (firewall LAN only).
